/* @flow */

import * as React from 'react';

type Props = {||};

const Level0: React.ComponentType<Props> = () => {
    return (
        <div>
            <div>Welcome!</div>
            <div>Here is the Smart Tribune frontend test !</div>
        </div>
    );
};

export default Level0;
