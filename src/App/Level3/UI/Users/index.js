/* @flow */

import * as React from 'react';
import List from './List';

type Props = {||};

const Users: React.ComponentType<Props> = () => (
    <section>
        <List />
    </section>
);

export default Users;
